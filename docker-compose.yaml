version: "3.9"

services:
  app:
    image: authentication-wo-devise-rails-api
    env_file:
      - .env
    ports:
      - "3000:3000"

  redis:
    image: redis:alpine
    command: redis-server --requirepass password
    ports:
      - "6379:6379"
    # volumes:
    #   - redis-data:/data
